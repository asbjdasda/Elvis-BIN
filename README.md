# Brozien Portfolio Static Site

Static export for `elvisbiozliner.onrender.com`.

## Deploy on Render

- Runtime: Static Site
- Build command: leave empty
- Publish directory: `.`

The entry file is `index.html`; bundled assets live in `assets/`.
